name: Create Board

on: [ push, pull_request ]

jobs:
  call-workflow-passing-data:
    uses: chuckwagoncomputing/hellen-one/.github/workflows/create-board.yaml@pass-directory
    with:
      working-directory: breakout-boards/Breakout_154pin_284617-1-Connector
    permissions:
      contents: write
